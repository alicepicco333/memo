# Local image caches — not needed for deployment (app uses CDN URLs)
images/
images_flat/
sampled_variants/
debug/

# Python data-processing source files (not served by the web app)
metadata.json
metadata_merged.json
classifications.json
gif_check_progress.json
variants_progress.json
meme_ontology.owl

# Python scripts & artifacts
__pycache__/
*.pyc
*.pyo

# Node
node_modules/
